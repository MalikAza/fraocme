[project]
name = "fraocme"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[project.scripts]
fraocme = "fraocme.cli:main"
test = "fraocme.test_runner:run"

[tool.uv]
package = true